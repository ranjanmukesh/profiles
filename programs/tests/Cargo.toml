[package]
name = "profiles-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
solana-program-test = "1.10"
solana-sdk = "1.10"
tokio = { version = "1", features = ["macros"]}
anchor-lang = "0.32.1"
profiles = { path = "../programs/profiles" }
